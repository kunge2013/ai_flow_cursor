<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiflow.server.mapper.KnowledgeBaseMapper">

    <!-- 基础结果映射 -->
    <resultMap id="BaseResultMap" type="com.aiflow.server.entity.KnowledgeBase">
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="vector_model" property="vectorModel" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="BOOLEAN"/>
        <result column="tags" property="tags" jdbcType="VARCHAR"/>
        <result column="config_json" property="configJson" jdbcType="VARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 分页查询知识库列表 -->
    <select id="selectKnowledgeBasePage" resultMap="BaseResultMap">
        SELECT *
        FROM t_knowledge_base
        WHERE deleted = 0
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="vectorModel != null and vectorModel != ''">
            AND vector_model = #{vectorModel}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC
    </select>

</mapper> 